headscale:
  url: http://localhost:8080
  preauth_key: "eb7860a3a47f47f86f2013cba0de0e01e082da9d7f35d88a"

ssh:
  port: 2222  # 使用非特权端口
  host_key_path: ./test_host_key
  authorized_keys_path: ./test_authorized_keys
  allowed_networks:
    - 100.64.0.0/10
    - 127.0.0.1/32

grpc:
  port: 50052  # 避免与 Headscale 冲突
  tls_enabled: false

device:
  name: TestDevice-MacOS
  
# 使用 localhost 进行演示
demo_mode:
  use_localhost: true
